diff a/src/desktop/src/ui/components/logos.js b/src/desktop/src/ui/components/logos.js	(rejected hunks)
@@ -1,7 +1,7 @@
-import React from "react";
-import css from "ui/components/logos.scss";
-import { withI18n } from "react-i18next";
-import { connect } from "react-redux";
-import classNames from "classnames";
-import logo from "ui/images/logo.png";
-import Icon from "ui/components/icon";
+import React from 'react';
+import css from 'ui/components/logos.scss';
+import { withI18n } from 'react-i18next';
+import { connect } from 'react-redux';
+import classNames from 'classnames';
+import logo from 'ui/images/logo.png';
+import Icon from 'ui/components/icon';
@@ -9,19 +9,19 @@ class Logos extends React.PureComponent {
-  render() {
-    return (
-      <div className={classNames(css.top_sec1)}>
-        <div className={classNames(css.lg_logos)}>
-          <img src={logo} alt="" />
-        </div>
-        {this.props.wallet.ready && (
-          <React.Fragment>
-          <span
-            style={{ float: "right", cursor: "pointer", marginRight:'50px'}}
-            onClick={() => this.props.history.push("/wallet/")}
-          >
-            <Icon icon="cross" size={18} />
-          </span>
-        </React.Fragment>
-        )}
-      </div>
-    );
-  }
+    render() {
+        return (
+            <div className={classNames(css.top_sec1)}>
+                <div className={classNames(css.lg_logos)}>
+                    <img src={logo} alt="" />
+                </div>
+                {this.props.wallet.ready && (
+                    <React.Fragment>
+                        <span
+                            style={{ float: 'right', cursor: 'pointer', marginRight: '50px' }}
+                            onClick={() => this.props.history.push('/wallet/')}
+                        >
+                            <Icon icon="cross" size={18} />
+                        </span>
+                    </React.Fragment>
+                )}
+            </div>
+        );
+    }
@@ -29,2 +29,2 @@ class Logos extends React.PureComponent {
-const mapStateToProps = state => ({
-  wallet: state.wallet
+const mapStateToProps = (state) => ({
+    wallet: state.wallet,
@@ -33,4 +33 @@ const mapStateToProps = state => ({
-
-export default connect(
-  mapStateToProps
-)(withI18n()(Logos));
+export default connect(mapStateToProps)(withI18n()(Logos));
