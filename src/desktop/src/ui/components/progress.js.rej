diff a/src/desktop/src/ui/components/progress.js b/src/desktop/src/ui/components/progress.js	(rejected hunks)
@@ -5,2 +5,2 @@ import css from './progress.scss';
-import { Progress} from 'react-sweet-progress';
-import "react-sweet-progress/lib/style.css";
+import { Progress } from 'react-sweet-progress';
+import 'react-sweet-progress/lib/style.css';
@@ -11 +11 @@ const circleProgress = {
-    width:'250px',
+    width: '250px',
@@ -13,5 +13,5 @@ const circleProgress = {
-    position:'absolute !important',
-    top:'100px',
-    left:'0px',
-    border:'none !important'
-}
+    position: 'absolute !important',
+    top: '100px',
+    left: '0px',
+    border: 'none !important',
+};
@@ -29,3 +29,3 @@ export default class ProgressBar extends React.PureComponent {
-    state={
-        color:'error'
-    }
+    state = {
+        color: 'error',
+    };
@@ -35,20 +35,28 @@ export default class ProgressBar extends React.PureComponent {
-        if(type==="send"){
-        if(Math.min(progress, 100)===0){
-            this.setState({
-                color:'error'
-            })
-        }
-        else if(Math.min(progress, 100)===25){
-            this.setState({
-                color:'default'
-            })
-        }
-        else if(Math.min(progress, 100)===50){
-            this.setState({
-                color:'active'
-            })
-        }
-        else if(Math.min(progress, 100)===100){
-            this.setState({
-                color:'success'
-            })
+        if (type === 'send') {
+            if (Math.min(progress, 100) === 0) {
+                this.setState({
+                    color: 'error',
+                });
+            } else if (Math.min(progress, 100) === 25) {
+                this.setState({
+                    color: 'default',
+                });
+            } else if (Math.min(progress, 100) === 50) {
+                this.setState({
+                    color: 'active',
+                });
+            } else if (Math.min(progress, 100) === 100) {
+                this.setState({
+                    color: 'success',
+                });
+            }
+        } else {
+            if (Math.min(Math.round(progress), 100) < 100) {
+                this.setState({
+                    color: 'default',
+                });
+            } else {
+                this.setState({
+                    color: 'success',
+                });
+            }
@@ -56,14 +64 @@ export default class ProgressBar extends React.PureComponent {
-    }
-    else{
-        if(Math.min(Math.round(progress),100)<100){
-        this.setState({
-            color:'default'
-        })
-    }
-    else{
-        this.setState({
-            color:'success'
-        })
-    }
-    }
-        
+
@@ -72 +66,0 @@ export default class ProgressBar extends React.PureComponent {
-                
@@ -77,9 +71,21 @@ export default class ProgressBar extends React.PureComponent {
-                <Progress type="circle" percent={Math.min(progress, 100)} status={this.state.color} style={circleProgress} theme={{
-                    default: {
-                        symbol: Math.round(progress) + '%',
-                        trailColor: '#152353',
-                        color: '#ebba53'
-                      }
-                }}/>
-                <br/><br/><br/><br/>
-                <br/><br/><br/><br/>
+                <Progress
+                    type="circle"
+                    percent={Math.min(progress, 100)}
+                    status={this.state.color}
+                    style={circleProgress}
+                    theme={{
+                        default: {
+                            symbol: Math.round(progress) + '%',
+                            trailColor: '#152353',
+                            color: '#ebba53',
+                        },
+                    }}
+                />
+                <br />
+                <br />
+                <br />
+                <br />
+                <br />
+                <br />
+                <br />
+                <br />
