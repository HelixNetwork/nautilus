diff a/src/desktop/src/ui/components/password.js b/src/desktop/src/ui/components/password.js	(rejected hunks)
@@ -2,5 +2,5 @@
-import React from "react";
-import PropTypes from "prop-types";
-import classNames from "classnames";
-import { translate } from "react-i18next";
-import { zxcvbn } from "libs/exports";
+import React from 'react';
+import PropTypes from 'prop-types';
+import classNames from 'classnames';
+import { translate } from 'react-i18next';
+import { zxcvbn } from 'libs/exports';
@@ -8 +8 @@ import { zxcvbn } from "libs/exports";
-import { passwordReasons } from "libs/password";
+import { passwordReasons } from 'libs/password';
@@ -10,2 +10,2 @@ import { passwordReasons } from "libs/password";
-import Icon from "ui/components/icon";
-import css from "./input.scss";
+import Icon from 'ui/components/icon';
+import css from './input.scss';
@@ -17,25 +17,25 @@ class PasswordInput extends React.PureComponent {
-  static propTypes = {
-    /** Current password value */
-    value: PropTypes.string.isRequired,
-    /** Should input focus when changed to true */
-    focus: PropTypes.bool,
-    /** Is the component disabled */
-    disabled: PropTypes.bool,
-    /** Should input show the password strength score */
-    showScore: PropTypes.bool,
-    /** Should input show the validation checkmark */
-    showValid: PropTypes.bool,
-    /** Should the input match a string */
-    match: PropTypes.string,
-    /** Password input label */
-    label: PropTypes.string.isRequired,
-    /** Password change event function
-     * @param {string} value - Current password value
-     */
-    onChange: PropTypes.func.isRequired,
-    /** Translation helper
-     * @param {string} translationString - locale string identifier to be translated
-     * @ignore
-     */
-    t: PropTypes.func.isRequired
-  };
+    static propTypes = {
+        /** Current password value */
+        value: PropTypes.string.isRequired,
+        /** Should input focus when changed to true */
+        focus: PropTypes.bool,
+        /** Is the component disabled */
+        disabled: PropTypes.bool,
+        /** Should input show the password strength score */
+        showScore: PropTypes.bool,
+        /** Should input show the validation checkmark */
+        showValid: PropTypes.bool,
+        /** Should the input match a string */
+        match: PropTypes.string,
+        /** Password input label */
+        label: PropTypes.string.isRequired,
+        /** Password change event function
+         * @param {string} value - Current password value
+         */
+        onChange: PropTypes.func.isRequired,
+        /** Translation helper
+         * @param {string} translationString - locale string identifier to be translated
+         * @ignore
+         */
+        t: PropTypes.func.isRequired,
+    };
@@ -43,4 +43,4 @@ class PasswordInput extends React.PureComponent {
-  state = {
-    hidden: true,
-    capsLock: false
-  };
+    state = {
+        hidden: true,
+        capsLock: false,
+    };
@@ -48,3 +48,7 @@ class PasswordInput extends React.PureComponent {
-  componentDidMount() {
-    if (this.props.focus) {
-      this.input.focus();
+    componentDidMount() {
+        if (this.props.focus) {
+            this.input.focus();
+        }
+        this.onKey = this.setCapsLock.bind(this);
+        window.addEventListener('keydown', this.onKey);
+        window.addEventListener('keyup', this.onKey);
@@ -52,4 +55,0 @@ class PasswordInput extends React.PureComponent {
-    this.onKey = this.setCapsLock.bind(this);
-    window.addEventListener("keydown", this.onKey);
-    window.addEventListener("keyup", this.onKey);
-  }
@@ -57,3 +57,4 @@ class PasswordInput extends React.PureComponent {
-  componentWillReceiveProps(nextProps) {
-    if (!this.props.focus && nextProps.focus) {
-      this.input.focus();
+    componentWillReceiveProps(nextProps) {
+        if (!this.props.focus && nextProps.focus) {
+            this.input.focus();
+        }
@@ -61 +61,0 @@ class PasswordInput extends React.PureComponent {
-  }
@@ -63,4 +63,4 @@ class PasswordInput extends React.PureComponent {
-  componentWillUnmount() {
-    window.removeEventListener("keydown", this.onKey);
-    window.removeEventListener("keyup", this.onKey);
-  }
+    componentWillUnmount() {
+        window.removeEventListener('keydown', this.onKey);
+        window.removeEventListener('keyup', this.onKey);
+    }
@@ -68,5 +68,5 @@ class PasswordInput extends React.PureComponent {
-  setVisibility = () => {
-    this.setState({
-      hidden: !this.state.hidden
-    });
-  };
+    setVisibility = () => {
+        this.setState({
+            hidden: !this.state.hidden,
+        });
+    };
@@ -74,5 +74,5 @@ class PasswordInput extends React.PureComponent {
-  setCapsLock = e => {
-    this.setState({
-      capsLock: e.getModifierState("CapsLock")
-    });
-  };
+    setCapsLock = (e) => {
+        this.setState({
+            capsLock: e.getModifierState('CapsLock'),
+        });
+    };
@@ -80,12 +80,3 @@ class PasswordInput extends React.PureComponent {
-  render() {
-    const {
-      label,
-      value,
-      disabled,
-      onChange,
-      showScore,
-      showValid,
-      match,
-      t
-    } = this.props;
-    const { hidden, capsLock } = this.state;
+    render() {
+        const { label, value, disabled, onChange, showScore, showValid, match, t } = this.props;
+        const { hidden, capsLock } = this.state;
@@ -93,3 +84,2 @@ class PasswordInput extends React.PureComponent {
-    const score = zxcvbn(value);
-    const isValid =
-      score.score === 4 && (typeof match !== "string" || match === value);
+        const score = zxcvbn(value);
+        const isValid = score.score === 4 && (typeof match !== 'string' || match === value);
@@ -97,48 +87 @@ class PasswordInput extends React.PureComponent {
-    return (
-      <div
-        className={classNames(
-          css.input,
-          css.padded,
-          disabled ? css.disabled : null,
-          capsLock ? css.capsLock : null
-        )}
-      >
-        <fieldset>
-          <a
-            className={hidden ? css.strike : null}
-            onClick={this.setVisibility}
-          >
-            <Icon icon="eye" size={16} />
-          </a>
-          <input
-            type={hidden ? "password" : "text"}
-            ref={input => {
-              this.input = input;
-            }}
-            value={value}
-            onClick={this.setCapsLock}
-            onKeyDown={this.setCapsLock}
-            onChange={e => onChange(e.target.value)}
-          />
-          <small>{label}</small>
-          <strong>
-            <Icon icon="warning" size={14} />
-            {t("capsLockIsOn")}
-          </strong>
-          {showScore ? (
-            <React.Fragment>
-              <div className={css.score} data-strength={score.score}>
-                <span />
-                <span />
-                <span />
-              </div>
-              {value.length > 8 && passwordReasons[score.feedback.warning] ? (
-                <div className={css.hint}>
-                  {t(
-                    `changePassword:${passwordReasons[score.feedback.warning]}`
-                  )}
-                </div>
-              ) : null}
-            </React.Fragment>
-          ) : null}
-          {showValid ? (
+        return (
@@ -146 +89,6 @@ class PasswordInput extends React.PureComponent {
-              className={classNames(css.valid, isValid ? css.isValid : null)}
+                className={classNames(
+                    css.input,
+                    css.padded,
+                    disabled ? css.disabled : null,
+                    capsLock ? css.capsLock : null,
+                )}
@@ -148 +96,39 @@ class PasswordInput extends React.PureComponent {
-              <Icon icon="tickRound" size={26} />
+                <fieldset>
+                    <a className={hidden ? css.strike : null} onClick={this.setVisibility}>
+                        <Icon icon="eye" size={16} />
+                    </a>
+                    <input
+                        type={hidden ? 'password' : 'text'}
+                        ref={(input) => {
+                            this.input = input;
+                        }}
+                        value={value}
+                        onClick={this.setCapsLock}
+                        onKeyDown={this.setCapsLock}
+                        onChange={(e) => onChange(e.target.value)}
+                    />
+                    <small>{label}</small>
+                    <strong>
+                        <Icon icon="warning" size={14} />
+                        {t('capsLockIsOn')}
+                    </strong>
+                    {showScore ? (
+                        <React.Fragment>
+                            <div className={css.score} data-strength={score.score}>
+                                <span />
+                                <span />
+                                <span />
+                            </div>
+                            {value.length > 8 && passwordReasons[score.feedback.warning] ? (
+                                <div className={css.hint}>
+                                    {t(`changePassword:${passwordReasons[score.feedback.warning]}`)}
+                                </div>
+                            ) : null}
+                        </React.Fragment>
+                    ) : null}
+                    {showValid ? (
+                        <div className={classNames(css.valid, isValid ? css.isValid : null)}>
+                            <Icon icon="tickRound" size={26} />
+                        </div>
+                    ) : null}
+                </fieldset>
@@ -150,5 +136,2 @@ class PasswordInput extends React.PureComponent {
-          ) : null}
-        </fieldset>
-      </div>
-    );
-  }
+        );
+    }
