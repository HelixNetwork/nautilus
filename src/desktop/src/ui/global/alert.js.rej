diff a/src/desktop/src/ui/global/alert.js b/src/desktop/src/ui/global/alert.js	(rejected hunks)
@@ -1,7 +1,7 @@
-import React from "react";
-import classNames from "classnames";
-import PropTypes from "prop-types";
-import { connect } from "react-redux";
-import { withI18n } from "react-i18next";
-import { dismissAlert } from "actions/alerts";
-import css from "./alerts.scss";
+import React from 'react';
+import classNames from 'classnames';
+import PropTypes from 'prop-types';
+import { connect } from 'react-redux';
+import { withI18n } from 'react-i18next';
+import { dismissAlert } from 'actions/alerts';
+import css from './alerts.scss';
@@ -13 +13 @@ class Alerts extends React.PureComponent {
-  static timeout = null;
+    static timeout = null;
@@ -15,12 +15,12 @@ class Alerts extends React.PureComponent {
-  static propTypes = {
-    /** @ignore */
-    dismissAlert: PropTypes.func.isRequired,
-    /** @ignore */
-    alerts: PropTypes.object.isRequired,
-    /** @ignore */
-    forceUpdate: PropTypes.bool.isRequired,
-    /** @ignore */
-    shouldUpdate: PropTypes.bool.isRequired,
-    /** @ignore */
-    t: PropTypes.func.isRequired
-  };
+    static propTypes = {
+        /** @ignore */
+        dismissAlert: PropTypes.func.isRequired,
+        /** @ignore */
+        alerts: PropTypes.object.isRequired,
+        /** @ignore */
+        forceUpdate: PropTypes.bool.isRequired,
+        /** @ignore */
+        shouldUpdate: PropTypes.bool.isRequired,
+        /** @ignore */
+        t: PropTypes.func.isRequired,
+    };
@@ -28,3 +28,3 @@ class Alerts extends React.PureComponent {
-  state = {
-    dismissUpdate: false
-  };
+    state = {
+        dismissUpdate: false,
+    };
@@ -32,3 +32,9 @@ class Alerts extends React.PureComponent {
-  componentWillReceiveProps(nextProps) {
-    if (this.timeout) {
-      clearTimeout(this.timeout);
+    componentWillReceiveProps(nextProps) {
+        if (this.timeout) {
+            clearTimeout(this.timeout);
+        }
+        if (nextProps.alerts.category && nextProps.alerts.category.length && nextProps.alerts.closeInterval > 0) {
+            this.timeout = setTimeout(() => {
+                this.props.dismissAlert();
+            }, nextProps.alerts.closeInterval);
+        }
@@ -36,10 +41,0 @@ class Alerts extends React.PureComponent {
-    if (
-      nextProps.alerts.category &&
-      nextProps.alerts.category.length &&
-      nextProps.alerts.closeInterval > 0
-    ) {
-      this.timeout = setTimeout(() => {
-        this.props.dismissAlert();
-      }, nextProps.alerts.closeInterval);
-    }
-  }
@@ -47,3 +43,5 @@ class Alerts extends React.PureComponent {
-  closeAlert() {
-    if (this.timeout) {
-      clearTimeout(this.timeout);
+    closeAlert() {
+        if (this.timeout) {
+            clearTimeout(this.timeout);
+        }
+        this.props.dismissAlert();
@@ -51,2 +48,0 @@ class Alerts extends React.PureComponent {
-    this.props.dismissAlert();
-  }
@@ -54,2 +50,2 @@ class Alerts extends React.PureComponent {
-  render() {
-    const { alerts, dismissAlert, t } = this.props;
+    render() {
+        const { alerts, dismissAlert, t } = this.props;
@@ -57,7 +53,7 @@ class Alerts extends React.PureComponent {
-    /**
-     * Temporarily override account fetch error by adding Proxy setting suggestion
-     */
-    const message =
-      alerts.message === t("invalidResponseFetchingAccount")
-        ? t("invalidResponseFetchingAccountDesktop")
-        : alerts.message;
+        /**
+         * Temporarily override account fetch error by adding Proxy setting suggestion
+         */
+        const message =
+            alerts.message === t('invalidResponseFetchingAccount')
+                ? t('invalidResponseFetchingAccountDesktop')
+                : alerts.message;
@@ -65,15 +61,15 @@ class Alerts extends React.PureComponent {
-    return (
-      <div className={css.wrapper}>
-        <div
-          onClick={() => dismissAlert()}
-          className={classNames(
-            alerts.category && alerts.category.length ? css.visible : null,
-            css[`${alerts.category}`]
-          )}
-        >
-          {alerts.title && <h2>{alerts.title}</h2>}
-          {message && <p>{message}</p>}
-        </div>
-      </div>
-    );
-  }
+        return (
+            <div className={css.wrapper}>
+                <div
+                    onClick={() => dismissAlert()}
+                    className={classNames(
+                        alerts.category && alerts.category.length ? css.visible : null,
+                        css[`${alerts.category}`],
+                    )}
+                >
+                    {alerts.title && <h2>{alerts.title}</h2>}
+                    {message && <p>{message}</p>}
+                </div>
+            </div>
+        );
+    }
@@ -82,4 +78,4 @@ class Alerts extends React.PureComponent {
-const mapStateToProps = state => ({
-  alerts: state.alerts,
-  forceUpdate: state.wallet.forceUpdate,
-  shouldUpdate: state.wallet.shouldUpdate
+const mapStateToProps = (state) => ({
+    alerts: state.alerts,
+    forceUpdate: state.wallet.forceUpdate,
+    shouldUpdate: state.wallet.shouldUpdate,
@@ -89 +85 @@ const mapDispatchToProps = {
-  dismissAlert
+    dismissAlert,
@@ -93,2 +89,2 @@ export default connect(
-  mapStateToProps,
-  mapDispatchToProps
+    mapStateToProps,
+    mapDispatchToProps,
